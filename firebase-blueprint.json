{
  "entities": {
    "Advisory": {
      "title": "Agricultural Advisory",
      "description": "Diagnosis and advice generated for a crop issue.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user ID of the farmer." },
        "cropType": { "type": "string", "description": "The identified plant type." },
        "diagnosis": { "type": "string", "description": "The full diagnosis text from Gemini." },
        "imageUrl": { "type": "string", "description": "URL to the image in Firebase Storage." },
        "createdAt": { "type": "string", "format": "date-time", "description": "ISO timestamp of when the advisory was created." },
        "status": { "type": "string", "enum": ["completed"], "description": "Status of the advisory." },
        "urgency": { "type": "string", "enum": ["low", "medium", "high"], "description": "Urgency level of the issue." },
        "recommendation": { "type": "string", "description": "Short summary of the recommendation." }
      },
      "required": ["uid", "cropType", "diagnosis", "imageUrl", "createdAt", "status"]
    }
  },
  "firestore": {
    "/advisories/{advisoryId}": {
      "schema": "Advisory",
      "description": "Collection of crop diagnoses and advisories."
    }
  }
}
